from src.parser import Parsing
import re


class Loading(Parsing):

    def challenger(self):
        chall = Parsing.challenger_filles(self) 
        for line in chall:
            








# call = Loading()
# test = call.challenger()
# print(test)